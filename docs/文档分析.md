**核心代码**
- `track_parameter_converter.py:36`/`499`/`569`/`624` 构建参数类型、若干 `BaseConverter` 子类（时间戳随机毫秒、track_id 匹配成 7001+ 序列、经纬度/速度合法性等）、`MessageBuilder` 以及 `TrackParameterConverter` 主流程，按照 `COLUMN_MAPPING` 校验每列、记统计并产出 `$RD` 报文，是整个模块的数据规范与转换内核。
- `track_converter_cli.py:22`/`96`/`119` 是面向终端的入口，解析输入/输出/日志/严格模式参数、先行校验文件可读，再调用 `create_converter()` 批量转换并打印成功率与日志提示，方便把 `*_smoothPoints.txt` 等原始文本快速变成 `.a3h`。
- `csv2a3h_improved.py:16`/`62`/`164` 提供功能完备的 CSV→A3H CLI：支持自定义输入输出、路径合法性检查、GBK 流式读写、跳过无效 ICAO Hex、以行号生成 second/ms/ns 三段时间戳，并为每条记录同步写 `$AP` / `$AV` 报文行。
- `csv2a3h.py:4`/`7`/`23`/`47`/`64` 是最初的快速脚本，硬编码文件名、以行号循环生成时间分量、字段换算英尺到米后写 `$AP`/`$AV`，便于阅读和与改进版本对照调试。

**辅助工具与资源**
- `extract_smooth_points.py:8`/`89` 依赖 `scipy.io`/`numpy` 打开 `track_20251210172235.mat`，遍历 `trackList` 中的 `smoothPointList`，把 `BatchNum` 与每个字段的标量值逐行写入 `*_smoothPoints.txt`，为 CLI 转换器提供可读文本输入。
- `compare_files.py:3`/`26` 使用 `difflib.unified_diff` 对比两份 `.a3h` 输出并提示行数差异，是校验不同算法或随机种子下报文一致性的轻量工具。
- 示例数据：`2026-02-04.csv` 支持 ADS-B→A3H 流程，`track_20251210172235.mat` 与 `track_20251210172235_smoothPoints.txt` 支撑 MAT→TXT→A3H 流程，可直接配套上述脚本做回归测试。
- `使用手册.md:1`/`3`/`9`/`12`/`16` 总结 CSV 转换、MAT 两步法以及多 A3H 合并（文中提到的 `integrate_files.py` 尚未包含在当前目录）和报文头部注释，作为操作说明。

**后续建议**
1. 依据当前示例数据，分别运行 `csv2a3h_improved.py` 与 `track_converter_cli.py`，确认输出 `.a3h` 报文结构符合下游需求，并保留 `compare_files.py` 的差异日志。
2. 如果需要 `integrate_files.py` 或更多报文格式，可以在 `track_parameter_converter.py` 中注册新的 `MessageFormat`/转换器，保持文档与代码同步更新。